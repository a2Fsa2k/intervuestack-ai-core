# OpenAI API key for local development.
# In production, set OPENAI_API_KEY (server-side env var in Vercel).
# VITE_OPENAI_API_KEY is still needed at build time (set to any non-empty value),
# but the actual key used in production comes from the server-side env var.
VITE_OPENAI_API_KEY=sk-...
